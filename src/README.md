# Source Directory

This directory is reserved for future source-code organization.

The original SeaKR code currently lives in the repository root and `SEAKR/`. Do not move it unless the import paths and scripts are updated deliberately.
