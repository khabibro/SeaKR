# Configs

Configuration files for local and university runs belong here.

Keep configs readable and avoid hardcoded absolute paths. Prefer environment variables for machine-specific locations.

- `paper.yaml`: strict manifest for reproducing the SeaKR paper setup.
- `university.yaml`: university-server path placeholders and runtime defaults.
