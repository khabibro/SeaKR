# Outputs Directory

Use this directory for generated experiment artifacts.

- `logs/`: runtime logs and debugging traces.
- `tables/`: summarized metrics and reproduction tables.
- `figures/`: plots and paper-style visualizations.
- `predictions/`: JSONL predictions and model outputs.

Logs and predictions can become large and are ignored by Git. Commit only small, final tables or figures when appropriate.
