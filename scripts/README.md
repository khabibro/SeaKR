# Scripts

Utility scripts for dataset preparation and experiment launch wrappers belong here.

Scripts should use environment variables such as `DATA_DIR`, `MODEL_DIR`, `OUTPUT_DIR`, and `HF_HOME` instead of absolute paths.
